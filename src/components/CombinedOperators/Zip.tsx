import React, { FC } from 'react'
import Wrapper from 'components/Common/Wrapper'
import { Button } from 'semantic-ui-react'

const Zip: FC = () => {
  return (
    <Wrapper title='zip'>
      <Button secondary>Zip</Button>
    </Wrapper>
  )
}

export default Zip
